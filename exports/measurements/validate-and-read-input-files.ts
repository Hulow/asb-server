export class ValidateAndReadInputFiles {
  // split into 2 flow: JSON and TXT
  //READ EACH FILE AND GET THE DATA
  //TXT: check if it's string, not empty
  //JSON: IF each field is not empty
  // RETURN THE DATA
}
